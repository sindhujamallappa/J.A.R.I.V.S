# --- Current stage: config + wake word + STT + intent + safety + execution ---
PyYAML>=6.0
python-dotenv>=1.0
numpy>=1.24,<2.0        # openWakeWord isn't fully NumPy-2 safe on Windows yet
sounddevice>=0.4.6      # mic capture; bundles PortAudio (no system install on Windows)
openwakeword>=0.6.0     # wake word; also pulls scipy, tqdm
onnxruntime>=1.16       # ONNX inference backend for openWakeWord
faster-whisper>=1.0     # STT; pulls ctranslate2 + tokenizers
ollama>=0.3             # intent parsing against the local Ollama server
send2trash>=1.8         # delete_file -> Recycle Bin (execution.delete_to_trash)
pycaw>=20240210         # master volume control; pulls comtypes
piper-tts>=1.3          # offline TTS (piper1-gpl; ships Windows wheels)

# --- Later stages (uncomment as each roadmap item lands) ---
# pyautogui>=0.9        # execution / GUI automation

# --- Optional extras ---
# speexdsp-ns           # required only if wake_word.enable_noise_suppression=true
